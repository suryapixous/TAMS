name: tams
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  convex_bottom_bar: ^3.0.0
  image_picker: ^1.0.0
  permission_handler: ^10.2.0
  table_calendar: ^3.0.0
  url_launcher: ^6.1.10  # Update to latest version
  android_intent_plus: ^4.0.0  # Update to latest version
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - Assets/Images/Python-Intrerview-Q&A-copy.webp
    - Assets/Images/logo-search-grid-2x.png
    - Assets/Images/ali-morshedlou-WMD64tMfc4k-unsplash.jpg
